#include "environment.h"
#include "sortapp.h"
#include "variadicapp.h"
#include <stdio.h>
//#include "binsearch.h"
#include "recursionapp.h"

int main() {
    //run(qsortApp);
    //run(qsortStringApp);
    //run(binSearchApp);
    //run(recursionSumApp);
    //run(notRecursionSumApp);
    //run(recursionMaxApp);
    //run(notRecursionMaxApp);
    run(variadicApp);
    return 0;
}
